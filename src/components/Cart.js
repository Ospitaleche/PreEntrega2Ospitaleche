import React from 'react';

const Cart = ({ cartItems }) => {
  return (
    <div className="cart">
      <h2>Shopping Cart</h2>
      {/* Display cart items and total */}
    </div>
  );
};

export default Cart;